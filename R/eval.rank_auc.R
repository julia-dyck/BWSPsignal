#




eval.rank_auc = function(aucs,
                         tte.dist.filter = c("w", "dw", "pgw"), # option to exclude/focus on specific tte distributions (after having a look at execution times/convergence/n_eff)
                         prior.dist.filter = c("fl", "fg", "ll", "gg")
                         # potentially add the other scenario pars as well
){
  require(dplyr)

  
  
  
  
  
}